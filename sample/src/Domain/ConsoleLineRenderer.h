
#pragma once
#include <iostream>
#include "./ConsoleLineBuffers.h"

class ConsoleLineRenderer {
public:
    //void render(std::ostream& out, ConsoleLineBuffers<char> const& lineBuffers){
    //    for (std::size_t i = 0; i < lineBuffers.lineCount(); ++i)
    //        out << "\x1b[2K\r" << 
    //}
};